# Supabaseプロジェクトの接続情報。実際の値はSupabaseダッシュボード(Project Settings > API)から取得し、
# .env.local (gitignore対象) にコピーして使用する。このファイル自体に実値を書き込まないこと。
#
# CLAUDE.md 1.1: サービス層はサーバー専用モジュール(lib/supabase/server.ts)でのみこれらを読み込む。
# NEXT_PUBLIC_ プレフィックスは付けない(クライアントバンドルに露出させないため)。

SUPABASE_URL=https://valzvncoqafajqwxedzc.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
